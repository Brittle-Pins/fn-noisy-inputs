## IDF Component Manager manifest for the main component.
## PlatformIO + ESP-IDF 5.x picks this up automatically during CMake configuration
## and downloads the component from the ESP Component Registry on the first build.
dependencies:
  espressif/esp-tflite-micro: ">=1.3.0"
  idf: ">=5.0.0"
