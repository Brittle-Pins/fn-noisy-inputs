# No external IDF components required.
# MLP inference uses exported float weights (include/mlp_weights.h) generated by
# mlp.py, so the TFLite Micro runtime is not needed on the device.
dependencies:
  idf: ">=5.0.0"
